<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.kedu.mapper.PopupMapper">
	<insert id="create">
		insert into tbl_popup(nm, hp, model, dt, rvTime)
					values(#{nm},#{hp},#{model},#{dt},#{rvTime})
	</insert>
</mapper>